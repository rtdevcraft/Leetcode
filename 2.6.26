class Solution(object):
    def canMakeArithmeticProgression(self, arr):
        arr.sort()
        expected_diff = arr[0] -arr[1]

        for i in range(len(arr)-1):
            diff = arr[i] -arr[i + 1]
            if expected_diff != diff:
                return False


        return True