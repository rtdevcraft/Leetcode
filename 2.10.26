class Solution(object):
    def smallestRepunitDivByK(self, k):
        n = 1
        for length in range(1, k + 1):
            if n % k == 0:
                return length
            n = n * 10  + 1
        return -1
       