class Solution(object):
    def addDigits(self, num):
        while len(str(num)) > 1:
            total = 0
            for digit in str(num):
                total = total + int(digit)
            num = total
        return num
            